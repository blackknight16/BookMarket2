//-----------------------------------------------------------------------
// Базовый репозиторий. Можно удалить.
//-----------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using System.Data.SqlClient;
using BookMarket.DbInfrastructure;
//using BookMarket.NewFolder1;

namespace BookMarket.Services
{
    public class BaseRepository //: //BookMarketDbContext
    {
        //protected BookMarketDbContext db;
        //protected SqlConnection conn;

        //Model1 sd = new Model1();

        public BaseRepository()
        {
            /*Addresses as1 = new Addresses();
            as1.Id = 34;
            sd.Addresses.Add(as1);
            sd.Addresses.
            sd.Database.ExecuteSqlCommand()
            sd.SaveChanges();*/

            //this.conn = new SqlConnection(SpecialConstants.CONNECTION_STRING);
            //this.db = new BookMarketDbContext(conn, true);

           
        }
    }
}